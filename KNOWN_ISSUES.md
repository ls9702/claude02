# 알려진 한계 (Known Issues)

M2(실시간 협업)·M3(오브젝트 댓글) 검증에서 확인했지만 이번 수정 라운드에서 고치지 않은 항목이다.
각 항목에 **왜 남겨 두는지**와 **언제 다시 볼지**를 적는다.

---

## 1. 탭을 즉시 닫으면 마지막 편집이 사라질 수 있다 (최대 800ms 창)

- **현상**: 마지막 편집 후 `SAVE_DEBOUNCE_MS`(현재 **800ms**, `frontend/src/collab/constants.ts`)
  안에 탭을 닫거나 다른 페이지로 즉시 이동하면 그 편집이 서버에 남지 않을 수 있다.
- **완화**: 디바운스를 1.5초 → 800ms 로 줄였고, `beforeunload`/`unload`/`pagehide`/`visibilitychange`
  네 경로에서 남은 저장을 밀어 넣는다(`Collab.flushSave`).
- **남는 한계**:
  - 브라우저는 `fetch(..., { keepalive: true })` 의 미해결 본문 합계를 **약 64KiB** 로 제한한다.
    그래서 씬이 크면(요소 수백 개, JSON 수백 KB) keepalive 요청 자체가 거절된다.
    이제 본문이 60KiB(`KEEPALIVE_MAX_BYTES`)를 넘으면 keepalive 대신 일반 fetch 로 보내지만,
    네비게이션이 진행 중인 요청을 취소할 수 있어 **완주가 보장되지 않는다**.
  - 즉, "편집 → 800ms 이내 탭 종료" 구간은 여전히 유실 가능 구간이다.
- **왜 지금 고치지 않는가**: 확실히 고치려면 (a) 쿠키 인증이 붙은 `sendBeacon` 전용 엔드포인트
  (POST + JSON 본문)를 새로 만들거나 (b) 이탈 시 변경분만 보내는 델타 저장이 필요하다.
  둘 다 서버 API 설계 변경이라 M2 수정 범위를 넘는다.
- **실사용 영향**: 배포 설정에서는 `VITE_DISABLE_PREVENT_UNLOAD` 를 켜지 않으므로,
  저장되지 않은 변경이 있으면 브라우저 기본 확인창(`preventUnload`)이 뜬다 —
  사용자가 곧바로 닫아도 그 사이에 플러시가 끝날 가능성이 높다.
- **다시 볼 시점**: 저장 API 를 손보는 마일스톤(M3 이후) 또는 대용량 씬 성능 작업 때.

## 2. 룸 서버가 죽었다 살아나면 재동기화까지 최대 20초쯤 걸린다

- **현상**: 릴레이(excalidraw-room)를 재시작하면 socket.io 클라이언트의 재접속 백오프
  때문에 약 16~20초 뒤에야 다시 붙는다. 그동안 다른 사람의 편집이 실시간으로 오지 않는다.
- **완화**: 이제 끊긴 동안 헤더 배지가 "재연결 중…" 으로 바뀌고,
  "변경 내용은 계속 저장됩니다" 안내 배너가 뜬다. **저장 경로(`PUT /scene`)는 릴레이와 무관**하므로
  데이터가 사라지지는 않으며, 재접속 후 서버 씬 병합으로 놓친 변경을 회수한다.
- **왜 지금 고치지 않는가**: 백오프 값(`reconnectionDelayMax` 등)은 업스트림 excalidraw 의
  검증된 기본값이다. 줄이면 릴레이 재기동 중 재접속 폭주로 이어질 수 있어 신중히 다뤄야 한다.
- **다시 볼 시점**: 운영에서 릴레이 재시작이 잦다고 판단되면.

## 3. 협업 소켓 강제 종료는 "사용자 단위" 다

- **현상**: 로그아웃·세션 멤버 해제·사용자 삭제·세션 잠금 시 서버가 그 사용자의
  열린 협업 소켓을 끊는다(`backend/src/collab/sockets.ts`). 다만 프록시는 소켓이 **어느 룸**에
  들어가 있는지 알 수 없어서(룸 참여는 소켓 위 socket.io 이벤트다) 그 사용자의 소켓을 **전부** 끊는다.
- **영향**: 다른 세션 페이지를 함께 열어 두었다면 그 페이지의 실시간 연결도 잠깐 끊겼다가
  자동으로 다시 붙는다(권한이 있으므로 재접속에 성공한다). 데이터 손실은 없다.
- **보조 장치**: 클라이언트도 `ROOM_RECHECK_MS`(기본 30초, `VITE_ROOM_RECHECK_MS` 로 조정)마다
  `GET /api/pages/:id/room` 을 다시 물어 403/401 이면 룸에서 나가고, `{locked:true}` 면 뷰 모드로 내려간다.
  따라서 훅이 놓친 경우에도 **최대 30초** 안에 정리된다.
- **다시 볼 시점**: 룸 참여 시점에도 멤버십을 검사하는 구조(아래 4번)로 바꿀 때 함께.

## 4. 룸 id 가 유출되면 무관한 로그인 사용자가 그 룸에 참여할 수 있다

- **현상**: 릴레이는 무상태라 "룸 id 를 아는가" 로만 참여를 통제한다(설계상 의도, `PLAN.md` §2.4).
  룸 id(20자 hex)를 어떤 경로로든 알게 된 **인증된** 사용자는 그 룸에 join 해서
  참가자 수·메시지 타이밍 같은 메타데이터를 관찰할 수 있다.
- **완화된 부분**: 룸 키가 없으면 내용은 복호화할 수 없고(AES-GCM),
  룸 키는 세션 멤버에게만, 잠기지 않은 세션에 한해 API 로만 전달된다(URL·localStorage 에 남지 않음).
  또한 조작된 페이로드로 다른 참가자 탭에서 예외를 일으키던 문제는
  `frontend/src/collab/payload.ts` 의 형태 검증으로 막았다.
- **왜 지금 고치지 않는가**: 근본 해결은 `join-room` 에 페이지 접근 토큰을 실어
  릴레이(또는 프록시)가 세션 멤버십을 확인하게 하는 것인데, 이는 릴레이 프로토콜 변경이다
  (M2 는 "릴레이는 프로토콜 그대로" 가 전제).
- **다시 볼 시점**: 릴레이를 손보는 마일스톤. 5인 이하 운영에서는 위험이 낮다.

## 5. 메인 앱에 Content-Security-Policy 가 없다

- **현상**: SPA 응답에 CSP 헤더가 없다. (업로드 이미지 서빙 `GET /files/:id` 에는 이미
  `default-src 'none'; sandbox` 가 걸려 있어 업로드 콘텐츠발 XSS 는 막혀 있다.)
- **이번에 한 것**: 모든 응답에 `X-Frame-Options: DENY` 를 붙였다(클릭재킹 방지).
- **왜 지금 고치지 않는가**: Excalidraw 는 인라인 스타일·워커·wasm 을 쓰기 때문에
  실효성 있는 CSP 는 배포 형태(리버스 프록시·정적 서빙 경로)가 확정된 뒤에 맞춰야 한다.
- **다시 볼 시점**: **M6 배포 단계** — 리버스 프록시 설정과 함께.

## 6. 잠긴 세션의 뷰어는 15초 주기 폴링으로 갱신된다

- **현상**: 잠긴 세션은 릴레이를 아예 쓰지 않으므로(그래야 읽기 전용이 실제로 보장된다),
  뷰어 화면은 `SCENE_POLL_MS`(기본 15초, `VITE_SCENE_POLL_MS`)마다 `GET /scene` 으로 갱신된다.
  관리자가 잠긴 보드를 고치면 뷰어 화면에는 최대 15초 뒤에 보인다.
- **판단**: 잠긴 보드는 "보관·읽기 전용" 용도라 실시간성이 필요 없다고 보고 의도적으로 둔 값이다.

## 7. 정보성 관찰 (수정 대상 아님)

- 동시 대량 편집 부하 중 `ApiError: 서버에 연결할 수 없습니다` 가 드물게 1회 관찰된다.
  자체 재시도로 즉시 회복되고 최종 데이터 정합성에는 영향이 없었다(m2-collab §9).
- 백엔드 프로세스 RSS 는 첫 라운드에 +25MB 늘고 이후 수렴한다(V8 워밍업). 누수 아님.
- 룸 서버는 조인/이탈 150회 동안 RSS 변화가 없었다(소켓 정리 정상).

## 8. 댓글 핀은 요소의 회전을 반영하지 않는다 (M3)

- **현상**: 요소 앵커 핀의 위치는 축 정렬 bounding box 의 우상단(`x + width`, `y`)이다.
  요소를 회전(`angle`)시켜도 핀은 회전 전 모서리에 남는다. 댓글 모드의 클릭 판정(어떤 요소에 붙일지)도
  같은 이유로 축 정렬 bbox 로만 한다 — 회전한 도형의 모서리 근처를 찍으면 빗나갈 수 있다.
- **왜 지금 고치지 않는가**: 정확히 하려면 요소 중심 기준 회전 변환을 앵커·히트테스트 양쪽에 넣어야 하는데,
  선(`linear`)·프레임처럼 bbox 가 실제 모양과 다른 타입까지 고려하면 Excalidraw 내부의 도형별 판정 로직을
  따라 들어가게 된다. 5인 규모 사용에서 체감 문제가 아니라고 판단했다.
- **다시 볼 시점**: 회전 도형을 자주 쓴다는 피드백이 오면 (`anchor.ts` 두 함수만 고치면 된다).

## 9. 고아 전환 좌표 저장은 "먼저 본 사람" 이 한다 (M3)

- **현상**: 앵커 요소가 삭제되면 그 순간 화면을 보고 있던 클라이언트가 `PATCH /api/comments/:id {x,y}` 로
  마지막 위치를 한 번 저장한다(브라우저마다 한 번, 값이 이미 같으면 보내지 않는다).
  두 사람이 동시에 보고 있으면 같은 값으로 두 번 저장될 수 있고, **아무도 보고 있지 않을 때** 지워지면
  저장 좌표는 다음에 누군가 그 페이지를 열 때 갱신된다.
- **영향**: 값이 같으므로 결과는 같고, 화면 표시는 언제나 씬에 남아 있는 삭제 요소의 마지막 위치를 쓰기 때문에
  저장이 늦어도 핀은 제자리에 있다. 씬에서 삭제 요소가 완전히 사라진 뒤(스냅샷 복원 등)에야 저장 좌표가 쓰인다.
- **왜 지금 고치지 않는가**: 서버가 씬 저장(`PUT /scene`)에서 삭제된 요소를 감지해 댓글 좌표를 갱신하는 편이
  깔끔하지만, 저장 경로에 댓글 테이블 쓰기를 얹는 결합이 생긴다. M3 범위 밖으로 둔다.
- **다시 볼 시점**: 씬 저장 API 를 손보는 마일스톤.

## 10. 댓글 소켓은 세션 잠금 시 끊지 않는다 (M3)

- **현상**: 로그아웃·멤버 해제·사용자 삭제·비밀번호 재설정에서는 그 사용자의 댓글 WebSocket 을 서버가 끊지만
  (`backend/src/comments/sockets.ts`), **세션 잠금** 에서는 끊지 않는다.
- **판단**: 잠긴 세션에서도 댓글 읽기와 해결 처리는 허용이므로 구독을 유지하는 편이 맞다.
  작성 차단은 REST 라우트가 매 요청 `assertWritable` 로 검사하므로 소켓이 열려 있어도 우회할 수 없다.
