# DS118 셀프호스팅 실시간 화이트보드 — 전체 스펙 (v2: 커스텀 래퍼 앱)

> **상태: 계획 확정, 구현 보류.** 사용자 검토 후 구현 시작 예정. (2026-09-01)
> v1(기성 오픈소스 조합 검토 이력)은 git 히스토리 참조.

## Context

Figma처럼 사진·그림·개념도를 그릴 수 있는 웹 기반 실시간 화이트보드를 개인 NAS(Synology DS118)에서 소규모(동시 5명 이하)로 운영한다. 오픈소스 Excalidraw를 캔버스 엔진으로 쓰되, 요구 기능(로그인 유지, 사용자별 세션 할당, 세션 내 다중 페이지, 오브젝트 댓글)은 순정 구성으로 불가능하므로 **`@excalidraw/excalidraw` npm 패키지를 임베드한 커스텀 래퍼 앱을 직접 개발**한다.

**확정된 결정사항**
- 하드웨어: DS118 (Realtek RTD1296, ARMv8 쿼드 A53 1.4GHz, RAM 1GB, DSM 7.0.1 → 7.2+ 업데이트 예정)
- 도메인: **`draw.863ad.co.kr`** + HTTPS
- 방식: 커스텀 래퍼 앱 (기존 "순정 Excalidraw + Basic Auth" 안은 요구 기능 미충족으로 폐기)
- ExcaliDash·AFFiNE은 RAM/ARM 제약, tldraw는 라이선스 유료화로 제외

---

## 1. 시스템 개요

| 항목 | 내용 |
|---|---|
| 목적 | 실시간 협업 화이트보드 (손그림·도형·개념도·이미지·댓글) |
| 동시 사용자 | 최대 5명 |
| 사용자 모델 | 관리자가 계정 발급·세션 개설·사용자를 세션에 할당. 사용자는 할당된 세션만 접근 |
| 클라이언트 | 웹 브라우저 (PC/태블릿/모바일, 펜 입력) |
| 서버 | DS118 Container Manager(도커, 커뮤니티 스크립트로 활성화), arm64 이미지 |

## 2. 기능 스펙

### 2.1 캔버스 (Excalidraw 엔진 기본 제공 — 개발 불필요)
- 글씨: 텍스트 도구, 폰트 3종(손글씨/일반/코드), 도형 안 텍스트, 화살표 라벨, 한국어 UI
- 그림: 손그림, 도형(사각형/원/다이아몬드/선), 지우개, 스타일(굵기/색/채우기/투명도), 이미지 붙여넣기·드래그앤드롭·크롭, 도형 라이브러리
- 확대/이동: 무한 캔버스, 줌/팬, 핀치 줌, 펜/터치 입력
- 자석 기능: 화살표 바인딩(도형에 자동 부착·추적), 오브젝트 스냅(정렬 가이드), 그리드 스냅, 엘보 화살표
- 기타: 프레임, 그룹화, 정렬/분배, z-order, 잠금, Alt+방향키 순서도 생성, Mermaid 변환, 레이저 포인터, 텍스트 검색, 다크 모드, 실행취소, PNG/SVG/.excalidraw 내보내기

### 2.2 인증/계정 (신규 개발)
- **로그인 페이지** (ID/비밀번호, bcrypt 해시 저장)
- **로그인 유지**: httpOnly 세션 쿠키 — 한번 로그인한 기기는 로그아웃 전까지 유지 (장기 만료, 예: 90일 슬라이딩)
- 로그아웃, 비밀번호 변경
- 역할: 관리자 / 일반 사용자

### 2.3 세션·페이지 관리 (신규 개발)
- **관리자**: 사용자 계정 생성/삭제, 세션(보드) 생성/삭제, **사용자 ↔ 세션 할당/해제**
- **사용자**: 로그인 후 자신에게 할당된 세션 목록 화면 → 클릭으로 진입, **세션 간 자유 전환** (예: A가 세션1·세션2에 할당되면 둘 다 왔다갔다 가능)
- **세션 내 다중 페이지**: 한 세션 아래 여러 페이지(캔버스), 페이지 탭/사이드바로 전환, 페이지 추가/이름변경/삭제/순서변경
- 접근 제어: 할당되지 않은 세션은 목록에도 안 보이고 URL 직접 접근도 차단 (서버 측 검증)

### 2.4 실시간 협업 (excalidraw-room 재사용 + 연동 개발)
- 페이지 단위 협업: 같은 페이지를 연 사용자끼리 실시간 동기화 (페이지당 룸 1개 자동 매핑)
- 참가자 커서·선택 영역 표시, 접속자 목록 표시
- 릴레이는 검증된 excalidraw-room을 그대로 사용 (무상태, 프로토콜 호환)

### 2.5 오브젝트 댓글 (신규 개발)
- 캔버스 요소(또는 캔버스 좌표)에 **댓글 핀** 부착 — 요소가 이동하면 핀도 따라감
- 핀 클릭 시 스레드(대댓글) 열람/작성, 작성자·시간 표시, 해결(resolve) 처리
- 사이드바에 페이지 댓글 목록, 실시간 반영(WebSocket)
- 구현: Excalidraw 캔버스 위 오버레이 레이어 + 백엔드 저장 (Excalidraw 본체 수정 없음 — 업스트림 업데이트 안전)

### 2.6 저장/백업
- 페이지 자동 저장: 편집 중 주기 저장 + 이탈 시 저장 (SQLite)
- 버전 스냅샷: 페이지별 최근 N개 보관, 복원 가능
- 내보내기: PNG/SVG/.excalidraw (Excalidraw 기본)
- 백업: SQLite 파일 + 업로드 이미지 디렉터리를 Task Scheduler로 정기 백업

## 3. 아키텍처

```
[브라우저]
   │ HTTPS (draw.863ad.co.kr)
   ▼
[nginx]  TLS 종단, 정적 프론트 서빙, /api·/ws 프록시          (~20MB)
   ├──> [app 백엔드]  Node.js+Fastify+SQLite: 인증(세션쿠키),   (~120MB)
   │      사용자/세션/페이지/댓글 API, 씬 저장, 댓글 WebSocket
   └──> [excalidraw-room]  캔버스 실시간 릴레이 (무상태)        (~60MB)
```
- 컨테이너 3개, 합계 약 200MB — RAM 여유(500~600MB) 내
- 프론트엔드: React + Vite + `@excalidraw/excalidraw` (SPA, nginx가 정적 서빙)
- DB: SQLite(better-sqlite3) — users, sessions, session_members, pages, scenes(스냅샷), comments, comment_replies
- 모든 이미지는 arm64로 PC에서 `docker buildx` 크로스 빌드

## 4. 네트워크/보안

- **도메인: `draw.863ad.co.kr`** → 집 공인 IP, 443 포트포워딩 + Let's Encrypt (도메인이 Cloudflare면 Tunnel 대안 가능). 프론트 빌드·nginx·백엔드 CORS/쿠키 도메인 설정에 이 값을 사용
- HTTPS 필수 (wss·클립보드 API 요구)
- 세션 쿠키: httpOnly + Secure + SameSite=Lax, 로그인 시도 rate limit
- DSM 관리 포트(5000/5001) 외부 비공개, 방화벽으로 443만 개방
- 업로드 크기 제한 (nginx + 백엔드 이중)

## 5. 사전 작업 (사용자 수행)

1. DSM 7.0.1 → 7.2+ 업데이트
2. SSH로 [007revad/ContainerManager_for_all_armv8](https://github.com/007revad/ContainerManager_for_all_armv8) 스크립트 실행 → Container Manager 설치, 패키지 자동 업데이트 제외 설정
3. `draw.863ad.co.kr` DNS A 레코드 + 공유기 443 포트포워딩

## 6. 저장소 산출물 구조

```
├── frontend/            # React+Vite+@excalidraw/excalidraw SPA
│   └── src/ (로그인, 세션목록, 페이지탭, 캔버스, 댓글 오버레이)
├── backend/             # Fastify+SQLite (auth, sessions, pages, comments API + WS)
├── room/                # excalidraw-room arm64 빌드 (업스트림 그대로)
├── nginx/nginx.conf
├── docker-compose.yml
├── build-arm64.sh       # PC에서 전체 이미지 크로스 빌드
├── SETUP.md             # NAS 배포 가이드
└── OPERATIONS.md        # 계정/세션 관리, 백업, 업데이트
```

## 7. 개발 마일스톤 및 규모

| 단계 | 내용 | 예상 |
|---|---|---|
| M1 | 백엔드 뼈대: 로그인(쿠키 유지)+사용자/세션/페이지 CRUD+할당, 프론트: 로그인·세션 목록·페이지 탭·캔버스 저장/불러오기 | 1주 |
| M2 | 실시간 협업: excalidraw-room 연동, 페이지↔룸 매핑, 접속자 표시 | 3~5일 |
| M3 | 오브젝트 댓글: 핀 오버레이, 스레드, 실시간 반영 | 1주 |
| M4 | arm64 빌드, docker-compose, NAS 배포, HTTPS, 가이드 문서 | 2~3일 |

총 개발 규모: **약 3~4주 (파트타임 기준)**.

## 7.5 개발 프로세스 — 모델 역할 분담

각 마일스톤을 아래 사이클로 진행 (메인 세션의 서브에이전트로 실행):

```
[Opus 5]  구현 담당 — 마일스톤 단위로 코드 작성
    ↓ 결과물
[Sonnet 5 × N]  기능 검증·테스트 담당 — 병렬로 각자 다른 영역을 테스트
    ↓ 버그 리포트 (재현 절차 포함)
[Opus 5]  버그 수정
    ↓ 수정본
[Fable 5 (메인 세션)]  최종 검토 — 요구사항 충족 여부, 코드 리뷰, 커밋/푸시 승인
```

- **Opus 5**: M1~M4의 실제 구현과 버그픽스 수행
- **Sonnet 5 여러 개(병렬)**: 영역별 분담 테스트 — ①인증/로그인 유지 ②세션 할당·접근 차단 ③페이지 CRUD·전환 ④실시간 협업 동기화 ⑤댓글 기능. 각자 버그 리포트 작성
- **Fable 5 (메인 세션)**: 오케스트레이션 + 요구사항 충족 검증, 리포트 취합, 수정 지시, 최종 리뷰 후 커밋
- 사이클 종료 조건: Sonnet 테스트 전원 통과 + Fable 검토 통과 → 다음 마일스톤 진행

## 8. 검증 방법

1. 로컬: `docker compose up` → 브라우저 2개(다른 계정)로 로그인 유지, 세션 할당·차단, 페이지 전환, 동시 편집, 댓글 실시간 반영 확인
2. arm64: buildx로 arm64 이미지 빌드 성공 확인 (QEMU 에뮬레이션 기동 스모크 테스트)
3. NAS: DS118 배포 후 HTTPS 접속, 재부팅 자동 기동, 5명 동시 편집 중 RAM 800MB 이하 확인
4. 백업/복원: SQLite 백업 후 새 볼륨에 복원해 데이터 무결성 확인

## 9. 리스크

- Container Manager 비공식 설치: DSM 메이저 업데이트 시 재실행 필요 가능
- RAM 1GB: 협업 5명 + 대형 이미지 다수 페이지에서 여유 축소 가능 → 스냅샷 보관 수 제한, 이미지 크기 제한으로 관리
- @excalidraw/excalidraw 버전 업그레이드 시 room 프로토콜 호환 확인 필요 → 프론트·room 버전 함께 고정
- 댓글 핀의 요소 추적: 요소 삭제 시 핀 처리(고아 댓글) 규칙 필요 — 삭제된 요소의 댓글은 좌표 고정으로 전환
