# OPERATIONS — 운영 안내서

배포가 끝난 뒤(→ `SETUP.md`) 일상적으로 하는 일을 모았다.
관리자 화면은 로그인 후 우측 상단 사용자 메뉴 → **관리** 에서 들어간다(`/admin`).

- [1. 계정](#1-계정)
- [2. 세션과 페이지](#2-세션과-페이지)
- [3. 백업과 복원](#3-백업과-복원)
- [4. 업데이트](#4-업데이트)
- [5. 로그 보기](#5-로그-보기)
- [6. AI 키와 요금](#6-ai-키와-요금)
- [7. 문제 해결](#7-문제-해결)

---

## 1. 계정

### 1-1. 계정 발급

**관리 → 사용자** 탭

1. 「사용자 추가」에 아이디·비밀번호(8자 이상)를 넣고 역할을 고른다(기본 **일반 사용자**).
2. 「사용자 추가」를 누르면 표에 바로 나타난다.
3. 발급한 비밀번호를 본인에게 전달한다.

> 관리자가 만든 계정은 **첫 로그인에서 비밀번호를 바꾸지 않아도 된다**.
> 강제 변경은 (a) 최초 부트스트랩 관리자와 (b) 아래의 비밀번호 재설정에만 걸린다.

### 1-2. 비밀번호 재설정

사용자 표의 **「비밀번호 재설정」** → 새 비밀번호를 입력한다. 이때 서버가 함께 하는 일:

- 그 사용자의 `must_change_password` 를 켠다 → 다음 로그인에서 반드시 새 비밀번호를 정하게 된다.
- **그 사용자의 로그인 세션을 전부 끊는다** (다른 기기에 남아 있던 로그인 포함).
- 열려 있던 협업·댓글·시트·세션 WebSocket 도 즉시 끊는다.

### 1-3. 역할 변경 · AI 허용 · 삭제

- **역할**: 표의 역할 드롭다운. 마지막 관리자의 역할은 바꿀 수 없다.
- **AI 허용**: 계정별 체크박스. 끄면 그 사용자는 ✨ 버튼도, `/api/ai/ask` 도 쓸 수 없다.
- **삭제**: 「삭제」 버튼. 자기 자신과 마지막 관리자는 지울 수 없다.
  삭제하면 그 사용자의 세션 할당·로그인 세션이 함께 사라지고 열린 소켓이 끊긴다.
  **그 사람이 그린 그림은 세션에 그대로 남는다**(그림은 페이지에 속하지 사람에게 속하지 않는다).

---

## 2. 세션과 페이지

### 2-1. 세션 개설과 할당

**관리 → 세션** 탭

1. 「세션 추가」에 이름을 넣고 만든다.
2. 세션 카드의 **멤버** 체크박스에서 참여자를 고른다. 체크하는 즉시 서버에 반영된다.
3. 할당된 사용자는 로그인 후 「내 세션」 목록에서 그 세션을 본다. 관리자는 모든 세션을 본다.

할당을 **해제**하면 그 사용자의 열린 협업·댓글·시트 소켓이 즉시 끊기고,
그 사람 화면은 세션 목록으로 나간다.

### 2-2. 페이지

페이지는 세션 화면 상단 탭에서 **「+ 페이지」** 로 추가한다.

- **그림판(캔버스)**: Excalidraw 페이지.
- **시트**: 표 페이지. 「빈 시트」 또는 「회비 장부」 템플릿을 고른다.

탭의 `◀ ▶` 로 순서를 바꾸고 `✕` 로 지운다. 페이지를 지우면 그 페이지의 씬·댓글·시트가 함께 사라지고,
어느 페이지에서도 더 쓰이지 않는 업로드 이미지는 디스크에서도 정리된다.

### 2-3. 잠금(= 보관)

세션 카드의 **「잠금(읽기 전용)」** 체크박스.

- 잠긴 세션은 **릴레이를 아예 쓰지 않는다** — 읽기 전용이 실제로 보장된다.
  일반 사용자는 보기만 되고, 저장·댓글 작성·시트 편집이 서버에서 막힌다.
- 잠긴 보드의 화면은 15초 주기 폴링으로 갱신된다(실시간이 아니다).
- 관리자는 잠긴 세션도 편집할 수 있다.
- 다 쓴 세션은 **지우지 말고 잠가서 보관**하는 것을 권한다. 지우면 되돌릴 수 없다.

### 2-4. 세션 삭제

세션 카드의 「삭제」. 확인 대화상자가 뜬다.
세션·페이지·씬·댓글·시트가 모두 사라지고, 다른 곳에서 쓰이지 않는 업로드 파일도 지워진다.
**되돌리려면 백업에서 복원하는 수밖에 없다** — 지우기 전에 3-1 의 백업을 한 번 돌려 둔다.

---

## 3. 백업과 복원

백업 대상은 두 가지다.

| 대상 | 위치 | 방법 |
|---|---|---|
| 데이터베이스 | `data/app.db` | `POST /api/admin/backup` (아래) |
| 업로드 이미지 | `data/files/` | 폴더 그대로 복사(rsync·Hyper Backup) |

### 3-1. 데이터베이스 백업 (`POST /api/admin/backup`)

관리자 권한으로 호출하면 SQLite 의 `VACUUM INTO` 로
`data/backup/app-<타임스탬프>.db` 를 만들고 **최신 7개만 남긴다**.

`VACUUM INTO` 를 쓰는 이유: 서비스를 멈추지 않고도 **정합성 있는 한 개의 파일**이 나온다.
WAL 모드에서 `app.db`/`app.db-wal`/`app.db-shm` 를 그냥 복사하면 복사 도중 체크포인트가 끼어 깨질 수 있다.

지금 남아 있는 백업 목록은 `GET /api/admin/backup` 으로 볼 수 있다.

### 3-2. DSM 작업 스케줄러로 자동화

**제어판 → 작업 스케줄러 → 생성 → 예약된 작업 → 사용자 정의 스크립트**

- 일반: 작업 이름 `whiteboard-backup`, 사용자 `root`
- 일정: 매일 새벽 4시 (사람이 안 쓰는 시간)
- 작업 설정 → 사용자 정의 스크립트:

```bash
#!/bin/bash
set -euo pipefail

BASE="https://draw.863ad.co.kr"
ADMIN_USER="admin"
ADMIN_PASS="여기에-관리자-비밀번호"
DATA="/volume1/docker/whiteboard/data"
JAR="$(mktemp)"

# 1) 로그인 → 세션 쿠키
curl -sS -c "$JAR" -H 'content-type: application/json' \
  -d "{\"username\":\"${ADMIN_USER}\",\"password\":\"${ADMIN_PASS}\"}" \
  "${BASE}/api/auth/login" > /dev/null

# 2) DB 백업 (VACUUM INTO, 최신 7개 유지)
curl -sS -b "$JAR" -X POST "${BASE}/api/admin/backup"
echo

# 3) 업로드 이미지 스냅샷 (증분)
rsync -a --delete "${DATA}/files/" "/volume1/backup/whiteboard/files/"

# 4) DB 백업본도 NAS 의 다른 폴더로 복사해 둔다
rsync -a --delete "${DATA}/backup/" "/volume1/backup/whiteboard/db/"

rm -f "$JAR"
```

> 스크립트에 비밀번호가 들어가므로 작업 소유자를 `root` 로 두고,
> 가능하면 `/volume1/backup` 을 Hyper Backup 으로 외부(USB·클라우드)에 한 번 더 보낸다.
> NAS 한 대 안의 복사본만으로는 디스크가 죽으면 같이 죽는다.

### 3-3. 복원

1. **Container Manager → 프로젝트 → whiteboard → 중지**
2. File Station 또는 SSH 로 파일을 되돌린다:

```bash
cd /volume1/docker/whiteboard/data
mv app.db app.db.broken            # 혹시 모르니 남겨 둔다
rm -f app.db-wal app.db-shm        # 예전 WAL 이 남아 있으면 안 된다
cp backup/app-20260905T040000.db app.db
chown 1000:1000 app.db

# 업로드 이미지도 되돌릴 때
rsync -a --delete /volume1/backup/whiteboard/files/ ./files/
chown -R 1000:1000 files
```

3. **프로젝트 → 시작**
4. `https://draw.863ad.co.kr/api/health` 가 `{"ok":true,...}` 인지 확인하고 로그인해 본다.

> 백업 파일은 `VACUUM INTO` 산출물이라 **WAL 파일이 필요 없다**. `app.db` 하나만 놓으면 된다.
> 백업 시점 이후의 편집은 사라진다.

---

## 4. 업데이트

1. PC 에서 최신 코드를 받고 `./build-arm64.sh` 를 다시 돌린다 → 새 태그의 tar 두 개.
2. tar 를 NAS 에 올리고 **Container Manager → 이미지 → 추가 → 파일에서 추가**.
3. **먼저 백업한다** (3-1 을 한 번 호출).
4. `docker/whiteboard/.env` 의 `APP_TAG` / `ROOM_TAG` 를 새 태그로 바꾼다.
5. **Container Manager → 프로젝트 → whiteboard → 빌드**(또는 중지 후 시작).
6. 확인:
   - `https://draw.863ad.co.kr/api/health` → `{"ok":true,"db":"ok","room":"ok",...}`
   - 세션 하나를 열어 그림·시트가 그대로인지 본다.

**DB 마이그레이션은 자동이다.** 앱이 기동할 때 `migrations` 테이블을 보고 밀린 것만 순서대로 적용한다
(`backend/src/db/schema.ts`). 그래서 업데이트 순서는 "백업 → 이미지 교체 → 시작" 이면 충분하다.

**되돌리려면** `.env` 의 태그를 예전 값으로 되돌리고 프로젝트를 다시 시작한다.
다만 새 버전이 이미 마이그레이션을 적용했다면 예전 이미지가 그 스키마를 못 읽을 수 있다 —
그때는 3-3 의 복원 절차로 백업 DB 까지 함께 되돌린다.

오래된 이미지는 **Container Manager → 이미지** 에서 지운다(디스크가 넉넉하지 않다).

---

## 5. 로그 보기

- **Container Manager → 컨테이너 → `whiteboard-app` → 세부 정보 → 로그**
  로그는 JSON 한 줄(pino) 형식이다. 봐야 할 것:
  - `"level":50` = error, `"level":40` = warn
  - `"msg":"incoming request"` / `"request completed"` 에 `statusCode` 와 `responseTime`(ms)
  - `"msg":"db backup"` = 백업이 돌았다
  - `"msg":"SIGTERM 수신 — 정상 종료를 시작합니다."` = 정상 종료(중지·업데이트)
- 로그는 컨테이너당 **5MB × 3개** 로 회전한다(`docker-compose.yml` 의 `logging`). 디스크가 차지 않는다.
- 릴레이(`whiteboard-room`)는 평소 조용하다. 기동 줄(`room: listening on ...`) 외에 뭔가 계속 찍힌다면 이상 신호다.

---

## 6. AI 키와 요금

- 키는 **서버에만** 둔다. `docker/whiteboard/.env` 의 `GEMINI_API_KEY` 뿐이고, 브라우저로 내려가지 않는다.
- 키를 바꾸면 **프로젝트를 다시 시작**해야 반영된다.
- 키를 비우면 AI 기능 전체가 꺼진다(✨ 버튼이 아예 나오지 않는다).
- **누가 쓸 수 있는가**: (1) 사용자 메뉴의 「AI 도우미 사용」 토글이 켜져 있고 (2) 서버에 키가 있고
  (3) 관리 화면에서 그 계정의 AI 허용이 켜져 있어야 한다. 셋 중 하나라도 아니면 버튼이 없다.
- **요금 주의**:
  - `AI_RATE_LIMIT_PER_MIN`(기본 20)은 **분당 폭주 방지 퓨즈**지 요금 상한이 아니다.
    프로세스 메모리에 세므로 컨테이너를 재시작하면 0부터 다시 센다.
  - 실제 상한은 Google Cloud 콘솔에서 **API 키의 할당량·예산 알림**으로 걸어야 한다.
  - 관리 화면 사용자 탭 위에 **최근 7일 호출 수**가 나온다. 갑자기 늘면 확인한다.
  - 「검색 기반」 체크박스를 켜면 Google 검색 그라운딩이 붙어 호출 단가가 올라간다.

---

## 7. 문제 해결

### 로그인이 유지되지 않는다 / 로그인 직후 다시 로그인 화면

세션 쿠키가 브라우저에 저장되지 않는 것이다.

1. 주소가 **`https://`** 인가? `COOKIE_SECURE=true` 면 HTTP 로는 쿠키가 붙지 않는다.
2. 리버스 프록시에 **`X-Forwarded-Proto: $scheme`** 헤더가 있는가? (SETUP 6-1)
3. `.env` 의 `PUBLIC_URL` 이 실제 접속 주소와 같은가?
4. 그래도 안 되면 브라우저 개발자 도구 → 네트워크 → 로그인 요청의 응답 헤더에서
   `Set-Cookie: sid=...; Secure` 가 오는지 본다.

### 실시간 협업이 안 된다 (다른 사람 그림이 안 보인다)

- 화면 상단 배지가 **「재연결 중…」** 이면 WebSocket 이 프록시를 못 지나는 것이다.
  → SETUP 6-1 의 **WebSocket 사용자 지정 헤더 프리셋**이 그 규칙에 붙어 있는지 확인한다.
  DSM 은 규칙을 편집해 저장할 때 헤더가 사라지는 일이 있으니 다시 열어 확인한다.
- 배지는 정상인데 그림만 안 온다면 브라우저 개발자 도구 → 네트워크 → WS 탭에서
  `/socket.io/` 연결이 101 로 올라갔는지 본다.

### 502 `room_unavailable`

app 이 릴레이에 못 붙는 것이다.

1. **Container Manager → 컨테이너**에서 `whiteboard-room` 이 실행 중인가? 다시 시작한다.
2. `docker-compose.yml` 의 `ROOM_URL` 이 `http://room:3002` 인가?
   (컨테이너 이름이 아니라 **서비스 이름** `room` 이어야 내부 DNS 가 푼다.)
3. `https://draw.863ad.co.kr/api/health` 의 `room` 값이 `"down"` 이면 같은 문제다.
   `"ok"` 인데도 502 가 난다면 릴레이가 떠 있지만 응답이 느린 것이다 — 로그를 본다.

> 릴레이가 죽어도 **저장은 계속된다**. 그림이 사라지지 않으니 당황하지 않아도 된다.
> 다만 실시간 반영이 멈추고, 릴레이를 살린 뒤 최대 20초쯤 지나야 다시 붙는다.

### 컨테이너가 계속 재시작한다

- **로그 첫 줄이 `SQLITE_CANTOPEN`**: `data/` 폴더 권한이다. SETUP 4-2 의 `chown 1000:1000` 을 실행한다.
- **로그 없이 죽는다 / `OOMKilled`**: 메모리 상한에 걸렸다.
  `docker-compose.yml` 의 `mem_limit` 을 올리고(app 384m → 512m) 프로젝트를 다시 시작한다.
  단, NAS 전체가 1GB 이므로 DSM 몫(약 350~450MB)을 남겨야 한다.
- **`ADMIN_PASSWORD` 관련 오류**: `.env` 에 값이 비어 있다. 8자 이상으로 채운다.

### 첫 로딩이 느리다

- 초기 번들이 크다(Excalidraw + 폰트). **두 번째 접속부터는 브라우저 캐시로 즉시** 뜬다 —
  해시가 붙은 자산은 1년 `immutable` 로 내려간다.
- 정적 파일은 빌드 때 미리 brotli/gzip 으로 압축해 두고 NAS 는 그대로 흘려보내기만 한다.
  NAS CPU 는 병목이 아니고, 보통 **가정용 회선의 업로드 대역폭**이 병목이다.
- 시트 페이지는 처음 열 때만 시트 엔진 청크를 따로 받는다(지연 로드).

### 브라우저 콘솔에 `Refused to load the font 'https://esm.sh/...'` 가 뜬다

**정상이다.** Excalidraw 는 `@font-face` 의 두 번째 후보로 항상 업스트림 CDN 을 적어 두는데,
우리는 폰트를 자체 호스팅하므로(첫 번째 후보) CSP 가 쓰지 않는 CDN 후보만 막는 것이다.
글씨는 NAS 에서 받은 폰트로 정상 표시된다. 자세한 내용은 `KNOWN_ISSUES.md` 19번.

### 그 밖에

증상이 여기 없으면 `KNOWN_ISSUES.md` 를 본다 — 의도적으로 남겨 둔 한계가 정리되어 있다
(마지막 편집 유실 창, 릴레이 재접속 지연, 잠긴 세션의 15초 폴링, 시트 수식 엔진의 제약 등).
